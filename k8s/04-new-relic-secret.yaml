apiVersion: v1
kind: Secret
metadata:
  name: new-relic
  namespace: fcg
type: Opaque
# stringData: valor em texto puro (o k8s codifica em base64).
# ATENCAO: valor de EXEMPLO. Substitua pela license key real do New Relic
# antes do deploy (nunca comite a chave real aqui).
stringData:
  license-key: "COLOQUE_SUA_CHAVE_AQUI"
